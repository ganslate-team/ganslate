project_dir: 
    - ./projects/maastro_lung_proton_cbct_to_ct
    - ./modules
train:
    mixed_precision: False
    cuda: True
    n_iters: 15000
    n_iters_decay: 15000
    batch_size: 1
    output_dir: /work/vq218944/checkpoints/structure_loss_reg
    seed: 24

    logging:
        freq: 100
        wandb:
            project: Thesis_CBCT
            image_filter: [0.289 ,0.40]
            run: structure_loss_reg

    checkpointing:
        freq: 2000

    dataset: 
        name: CBCTtoCTDataset
        root: /work/vq218944/maastro_physics/train
        num_workers: 8
        patch_size: [16, 224, 224]

    gan:  
        name: CycleGANwStructure

        generator:
            name: Vnet3D
            in_channels: 1
            out_channels: 1
            down_blocks: [2, 2, 3]
            up_blocks: [3, 3, 3]

        discriminator:
            name: PatchGAN3D
            n_layers: 2
            in_channels: 1

        optimizer:
            lambda_AB: 5.0
            lambda_BA: 5.0
            lambda_identity: 0.0
            proportion_ssim: 0.0
            lambda_structure: 1
            structure_criterion: "Registration"
            lr_D: 0.0002
            lr_G: 0.0004

    metrics: 
        discriminator_evolution: True
        ssim: True
    
val:
    freq: 2000
    start_after: 10000

    multi_dataset:
        lungs:
            name: CBCTtoCTValTestDataset
            root: /work/vq218944/maastro_physics/val
        
        phantoms:
            name: CBCTtoCTValTestDataset
            root:  /work/vq218944/maastro_physics/val_phantom

    sliding_window:
        window_size: ${train.dataset.patch_size}
